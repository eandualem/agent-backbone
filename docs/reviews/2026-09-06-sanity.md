# Post-backlog sanity check — 2026-09-06

Follow-up inspection of the merged audit backlog, including delivery receipts,
review/closure replay, agent registration, live configuration, runtime metadata,
queue snapshots, package boundaries and test resource cleanup.

The earlier recommendations R1–R10 are implemented by PRs #163, #164, #165 and
#169. Swarm startup, terminal targeting and provider-state fixes landed in the
associated backlog PRs. This is a targeted integration check, not a claim that
every possible runtime or provider failure has been reproduced.

## Findings addressed

| Finding | Change and evidence |
| --- | --- |
| Current-issue comments bypassed readiness, including provider failures and human dialogs. | Removed the exception and its matching helper. Comments use the common queue gate. Regression cases cover busy, starting, quota, provider, permission, plan and question states, with and without priority, and delivery after returning to idle. Dedicated plan responses retain their verified plan-only path. |
| Discovery produced names rejected by shared validation for directories beginning with underscores. | Normalize leading punctuation before registration; preserve valid internal/trailing underscores. Real-store tests cover `_app`, mixed punctuation and punctuation-only names. |
| Arbitrary label queries performed an unrelated complete queue fetch first. | Delegate immediately with the original filters and pagination. The regression fails if any extra request is made. Removed the unused snapshot timestamp and its test-only assertion. |
| Routing imported two private agent modules across package boundaries. | Use the existing public exports. The import graph check now rejects cross-package private-module imports. |
| A PTY test leaked an unawaited coroutine and a slave file descriptor. | Await the real task with a mocked reader, and let normal cleanup close the real descriptors. Focused checks pass with runtime/unraisable warnings treated as errors. |

## Verification

Full `make check`: 1,542 tests pass with lint/format clean and no warnings.
A wheel and source distribution build successfully. `make smoke` passes against
real tmux (paste, keys, capture, display and cleanup). A real `backbone tell`
through a scratch API reached a disposable shell agent; both were stopped after
verification. Tests use scratch data or
in-memory SQLite; they do not alter the installed database or integration settings.
