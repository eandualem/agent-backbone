"""Workflow templates — composable multi-step automation flows.

Discovered automatically by WorkflowRegistry. Each module defines a single
@flow-decorated function whose name matches the module filename.
"""
